/*import { Navigate, useNavigate } from "react-router-dom";
import '../style/CartIndicator.css';
import { GiSushis } from "react-icons/gi";
import { useSelector } from "react-redux";

export default function CartIndicator (){
    const CartIndicator = () => {
        const Navigate = useNavigate()

        const cartLenght = useSelector((state) => state.cart.content.lenght)
        // cartLenght inizialmente è zzero ma rispechhierà in qualsiasi momento la lunghezza di cart.content
    }
    return (
        <div className="buttonStore">
            <button type="button" onClick={() => Navigate ('/cart')} class="btn btn-secondary">
                <GiSushis className="IconSus"/>
                0
                </button>
        </div>
    )
}
*/
